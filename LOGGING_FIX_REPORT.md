# 📊 Отчет о системе логирования использования

> **Статус:** ⚠️ **КРИТИЧЕСКАЯ ПРОБЛЕМА НАЙДЕНА И ИСПРАВЛЕНА**

## Проблема (найдена и зафиксирована)

**Проблема:** Счетчик обращений и токенов не работает  
**Причина:** Middleware логирования отключена в DEBUG режиме  
**Файл с проблемой:** `app/main.py` (строка 97)  
**Статус:** ✅ **ИСПРАВЛЕНА**

## 1. Анализ найденных проблем

### Проблема 1: Middleware отключена в DEBUG режиме ✅ ИСПРАВЛЕНА

**Файл:** `app/main.py`  
**Строки:** 94-97

**ДО (неработающий код):**
```python
# Usage tracking middleware (non-blocking, low priority)
if not settings.debug:
    app.add_middleware(UsageMiddleware)
```

**ПОСЛЕ (исправленный код):**
```python
# Usage tracking middleware (non-blocking, low priority)
# Note: Always enabled to track API usage regardless of debug mode
# Usage logging is essential for billing and analytics
app.add_middleware(UsageMiddleware)
```

**Объяснение:**
- Settings.debug = true (из .env файлов)
- Условие `if not settings.debug:` = false → middleware НИКОГДА не регистрируется
- Результат: **Все API запросы игнорируются, логирование полностью отключено**

**Решение:** Удалено условие, middleware теперь всегда активна

---

### Проблема 2: Счетчики хранятся в памяти (IN-MEMORY) ⏳ ТРЕБУЕТ РЕШЕНИЯ

**Файл:** `app/policy/engine.py`  
**Строка:** 20  
**Класс:** `PolicyEngine`

```python
class PolicyEngine:
    def __init__(self):
        # Rate limiter хранится в памяти процесса
        self._rate: dict[Tuple[int, str], Tuple[float, int]] = {}
```

**Проблемы:**
- Данные теряются при перезагрузке приложения
- Не работает в multi-instance deployments (Docker Swarm, Kubernetes)
- Нет persistent storage

**Текущее использование:**
```python
# Lines 224-260: increment_usage()
ba.free_requests_used = free_requests_used  # Обновляет БД
ba.requests_used_current_period = requests_used_current_period

# Lines 269-280: reset_period_if_needed()
# Сбрасывает счетчики по расписанию
```

**Рекомендация:** Перенести в Redis или PostgreSQL

---

### Проблема 3: Токены НЕ логируются в UsageRecord ⏳ ТРЕБУЕТ РЕШЕНИЯ

**Файл:** `app/usage/tracker.py`  
**Строки:** 61-76

```python
# Middleware ВСЕГДА логирует tokens=0 и cost=0
usage_record = UsageRecord(
    user_id=user_id,
    endpoint=scope["path"],
    method=scope["method"],
    status_code=response_status,
    response_time_ms=int((time.time() - start_time) * 1000),
    total_tokens=0,  # ❌ ВСЕГДА 0! Middleware не имеет доступа к LLM данным
    cost=0,  # ❌ ВСЕГДА 0
)
```

**Почему:** Middleware перехватывает HTTP запрос/ответ, но не имеет доступа к OpenAI response

**Текущее логирование токенов:**
- `app/policy/engine.py` → `increment_usage()` (НЕ логирует в UsageRecord)
- `app/agents/router.py` → вызывает `policy_engine.increment_usage()` (строка 159)

**Проблема:** Токены обновляют только `BillingAccount`, но не `UsageRecord`

**Рекомендация:** Логировать токены в `agents/router.py` после выполнения агента

---

## 2. Решения

### ✅ РЕШЕНИЕ 1: Активировать Middleware (ПРИМЕНЕНО)

**Статус:** ГОТОВО  
**Действие:** Удалено условие `if not settings.debug:` из `app/main.py`  
**Эффект:** Все API запросы теперь логируются в таблицу `usage_records`

**Проверка:**
```sql
SELECT * FROM usage_records 
ORDER BY created_at DESC LIMIT 10;
```

---

### ⏳ РЕШЕНИЕ 2: Persistent Rate Limiter

**Вопрос:** Какой storage использовать?

**Вариант A: Redis** (Рекомендуется для production)
```python
# В PolicyEngine.__init__()
self.redis = redis.Redis(host=settings.redis_host, port=6379)

# В check_usage_limits()
key = f"rate:{user_id}:{interval}"
count = self.redis.get(key)
self.redis.expire(key, ttl_seconds)
```

**Вариант B: PostgreSQL** (Если Redis недоступен)
```python
# Новая таблица: rate_limits
CREATE TABLE rate_limits (
    user_id INT,
    interval_key VARCHAR,
    count INT,
    expires_at TIMESTAMP,
    PRIMARY KEY (user_id, interval_key)
);
```

**Вариант C: Оставить в памяти** (Для разработки)
- Простое решение
- Не масштабируется
- Теряет данные при перезагрузке

**Ваш выбор?** → `settings.rate_limiter_backend` (redis|database|memory)

---

### ⏳ РЕШЕНИЕ 3: Логировать Токены

**Где добавить логирование:** `app/agents/router.py` (строка 159)

**Текущий код:**
```python
response = await agent_runtime.invoke(...)
policy_engine.increment_usage(...)
```

**Улучшенный код:**
```python
response = await agent_runtime.invoke(...)

# Логировать токены в UsageRecord
usage_log = UsageRecord(
    user_id=current_user.id,
    endpoint=f"/agents/{agent_id}/invoke",
    method="POST",
    status_code=200,
    total_tokens=response.get('usage', {}).get('total_tokens', 0),
    cost=calculate_cost(response.get('usage')),
    created_at=datetime.utcnow()
)
db.add(usage_log)

# Обновить счетчики в BillingAccount
policy_engine.increment_usage(...)
```

---

## 3. Архитектура логирования

```
HTTP Request
    ↓
┌─────────────────────────────────────────────┐
│ UsageMiddleware (app/usage/tracker.py)      │
│ - Логирует: endpoint, method, response_time │
│ - Хранит: UsageRecord в БД                  │
│ - ПРОБЛЕМА: tokens всегда 0                 │
└─────────────────────────────────────────────┘
    ↓
API Endpoint
    ↓
┌─────────────────────────────────────────────┐
│ PolicyEngine (app/policy/engine.py)         │
│ - Проверяет лимиты                          │
│ - Обновляет BillingAccount.requests_used    │
│ - ПРОБЛЕМА: Rate limiter в памяти           │
│ - ПРОБЛЕМА: Не логирует UsageRecord         │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ AgentRuntime (app/agents/runtime.py)        │
│ - Выполняет LLM запрос                      │
│ - Получает token data из OpenAI             │
│ - ПРОБЛЕМА: Не логирует результат           │
└─────────────────────────────────────────────┘
    ↓
Результат: UsageRecord + BillingAccount обновлены
```

---

## 4. Данные логирования

### Таблица: `usage_records`
```python
class UsageRecord(Base):
    __tablename__ = "usage_records"
    
    id: int
    user_id: int  # ← Кто сделал запрос
    endpoint: str  # ← /agents/123/invoke
    method: str  # ← POST, GET
    status_code: int  # ← 200, 400, 500
    response_time_ms: int  # ← Время ответа
    total_tokens: int  # ← ❌ ВСЕГДА 0 (Middleware не имеет доступа)
    cost: float  # ← ❌ ВСЕГДА 0
    error_message: str | None  # ← Если status >= 400
    created_at: datetime
```

### Таблица: `billing_accounts`
```python
class BillingAccount(Base):
    free_requests_used: int  # ← Обновляется policy_engine
    requests_used_current_period: int  # ← Обновляется policy_engine
    period_started_at: datetime  # ← Для сброса счетчика
```

---

## 5. Проверка логирования

### 1️⃣ Убедиться, что middleware включена

```bash
# Проверить, что исправление применено
grep -n "app.add_middleware(UsageMiddleware)" app/main.py
# Должно быть БЕЗ "if not settings.debug:" перед ней
```

### 2️⃣ Перезагрузить приложение

```bash
# Если используете systemd
sudo systemctl restart bot-generic

# Если используете Docker
docker-compose restart app

# Если используете PM2
pm2 restart all
```

### 3️⃣ Проверить данные в БД

```bash
# Подключиться к PostgreSQL
psql -h localhost -U postgres -d bot_generic

# Выполнить запросы из CHECK_LOGGING.sql
\i CHECK_LOGGING.sql
```

### 4️⃣ Протестировать вручную

```bash
# Сделать API запрос
curl -H "Authorization: Bearer YOUR_TOKEN" \
     http://localhost:8000/agents/1/invoke

# Проверить логи
psql -c "SELECT * FROM usage_records ORDER BY created_at DESC LIMIT 5;"
```

---

## 6. Следующие шаги

### 🔴 КРИТИЧЕСКИЙ (сделан):
- [x] Включить middleware логирования
- [x] Перезагрузить приложение

### 🟡 ВАЖНЫЙ (нужно решение):
- [ ] Выбрать: Redis, Database или Memory для rate limiter?
- [ ] Добавить логирование токенов в `agents/router.py`?

### 🟢 ОПЦИОНАЛЬНЫЙ (улучшения):
- [ ] Добавить Dashboard для визуализации использования
- [ ] Настроить алерты при превышении лимитов
- [ ] Экспортировать логи в Elasticsearch/CloudWatch

---

## 7. Вопросы для уточнения

1. **Где работает приложение?**
   - Localhost?
   - VPS?
   - Docker?
   - Kubernetes?
   
   → Зависит выбор rate limiter storage

2. **Нужны ли токены в analytics?**
   - Если да → логировать в UsageRecord
   - Если нет → оставить как есть
   
3. **Запущено ли несколько instance'ов приложения?**
   - Если да → нужно Redis или Database
   - Если нет → можно оставить в памяти

---

## 8. Файлы, которые надо проверить

| Файл | Проблема | Статус |
|------|----------|--------|
| `app/main.py` | Middleware отключена в DEBUG | ✅ ИСПРАВЛЕНА |
| `app/usage/tracker.py` | Tokens всегда 0 | ⏳ Требует решения |
| `app/policy/engine.py` | Rate limiter в памяти | ⏳ Требует решения |
| `app/agents/router.py` | Не логирует токены | ⏳ Требует решения |

---

## 9. Команды для диагностики

```bash
# 1. Проверить, что middleware активна
ps aux | grep python
# Должны быть запущены с активной middleware

# 2. Проверить логи приложения
tail -f /var/log/bot-generic.log
# Или если в Docker:
docker logs -f bot-generic

# 3. Проверить процессы памяти
free -h
# Убедиться, что достаточно памяти для rate limiter

# 4. Проверить подключение к БД
psql -h localhost -U postgres -d bot_generic -c "SELECT 1;"
# Должно вернуть успешный результат

# 5. Проверить размер таблицы usage_records
psql -h localhost -U postgres -d bot_generic -c "SELECT pg_size_pretty(pg_total_relation_size('usage_records'));"
```

---

**Статус:** ✅ Middleware активирована, готова к перезагрузке приложения  
**Дата анализа:** 2024  
**Автор:** Automated System Analysis  
