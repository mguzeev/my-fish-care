# Manage Admin Script

Скрипт для управления правами администратора пользователей по Telegram ID.

## Использование

### Установить права администратора
```bash
python scripts/manage_admin.py grant <telegram_id>
```

Пример:
```bash
python scripts/manage_admin.py grant 123456789
```

Вывод:
```
✅ Права администратора установлены для пользователя:
   Email: user@example.com
   Имя: John Doe
   Telegram ID: 123456789
   Telegram Username: @johndoe
```

### Удалить права администратора
```bash
python scripts/manage_admin.py revoke <telegram_id>
```

Пример:
```bash
python scripts/manage_admin.py revoke 123456789
```

Вывод:
```
✅ Права администратора удалены у пользователя:
   Email: user@example.com
   Имя: John Doe
   Telegram ID: 123456789
   Telegram Username: @johndoe
```

### Проверить статус пользователя
```bash
python scripts/manage_admin.py check <telegram_id>
```

Пример:
```bash
python scripts/manage_admin.py check 123456789
```

Вывод:
```
📋 Информация о пользователе:
   Email: user@example.com
   Имя: John Doe
   Username: johndoe
   Telegram ID: 123456789
   Telegram Username: @johndoe
   Статус: 🔓 Активен
   Верификация: ✓ Подтвержден
   Администратор: ✓ Да
   Роль в организации: admin
   Создан: 2026-01-11 10:30:45
```

### Показать список всех администраторов
```bash
python scripts/manage_admin.py list
```

Вывод:
```
👥 Список администраторов (2):
================================================================================

1. admin@example.com
   Имя: Admin User
   Telegram: @adminuser (ID: 111111111)
   Статус: 🔓 Активен
   Создан: 2026-01-10 09:00:00

2. superadmin@example.com
   Имя: Super Admin
   Telegram: @superadmin (ID: 222222222)
   Статус: 🔓 Активен
   Создан: 2026-01-11 10:00:00

================================================================================
```

## Требования

- Python 3.10+
- Активное виртуальное окружение
- Доступ к базе данных (настроенный в `.env`)

## Запуск

Всегда запускайте скрипт из корневой директории проекта с активированным виртуальным окружением:

```bash
# Активировать виртуальное окружение
source .venv/bin/activate

# Запустить скрипт
python scripts/manage_admin.py <command> [telegram_id]
```

## Как найти Telegram ID?

1. **Через бота:** Попросите пользователя отправить команду `/start` вашему боту - в логах будет виден его Telegram ID
2. **Через @userinfobot:** Пользователь может отправить `/start` боту @userinfobot в Telegram
3. **Через базу данных:** Проверьте таблицу `users`, колонку `telegram_id`

## Безопасность

⚠️ **Важно:** Этот скрипт предоставляет полные права администратора. Используйте с осторожностью!

- Храните скрипт в безопасном месте
- Не давайте доступ к скрипту неавторизованным пользователям
- Логируйте все изменения прав администратора
- Регулярно проверяйте список администраторов

## Коды выхода

- `0` - Успешное выполнение
- `1` - Ошибка (пользователь не найден, неверные параметры, и т.д.)

## Примеры

### Сценарий 1: Назначить нового администратора
```bash
# Проверить текущий статус
python scripts/manage_admin.py check 123456789

# Установить права админа
python scripts/manage_admin.py grant 123456789

# Проверить что права установлены
python scripts/manage_admin.py check 123456789
```

### Сценарий 2: Отозвать права администратора
```bash
# Посмотреть список всех администраторов
python scripts/manage_admin.py list

# Удалить права админа
python scripts/manage_admin.py revoke 123456789

# Проверить что права удалены
python scripts/manage_admin.py list
```

### Сценарий 3: Массовое управление
```bash
# Создать скрипт для массового назначения
for id in 111111111 222222222 333333333; do
    python scripts/manage_admin.py grant $id
done

# Проверить результат
python scripts/manage_admin.py list
```

## Troubleshooting

### Ошибка "Пользователь не найден"
- Убедитесь что пользователь зарегистрирован в системе
- Проверьте что пользователь привязал свой Telegram аккаунт
- Убедитесь что указан правильный Telegram ID

### Ошибка "Module not found"
```bash
# Активируйте виртуальное окружение
source .venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### Ошибка подключения к базе данных
- Проверьте файл `.env` и настройки `DATABASE_URL`
- Убедитесь что база данных запущена
- Проверьте права доступа к базе данных
