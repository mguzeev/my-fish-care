# 🚨 АУДИТ БИЛЛИНГОВОЙ СИСТЕМЫ - КРИТИЧЕСКИЕ ПРОБЛЕМЫ

**Дата аудита**: 18 января 2026  
**Статус**: КРИТИЧЕСКИЕ ПРОБЛЕМЫ ОБНАРУЖЕНЫ ⚠️

---

## 📋 КРАТКОЕ РЕЗЮМЕ

**Обнаружено 9 критических и важных проблем** в системе биллинга и подписок:

- 🔴 **5 критических проблем** - требуют немедленного исправления
- 🟡 **2 структурных проблемы** - влияют на архитектуру 
- 🔴 **2 отсутствующие проверки** - угрожают бизнес-логике

---

## 1. ✅ РЕГИСТРАЦИЯ ПОЛЬЗОВАТЕЛЯ - РАБОТАЕТ КОРРЕКТНО

**Код**: [app/auth/router.py](app/auth/router.py#L88-L130)

✅ При регистрации пользователю автоматически создаётся план "Free Trial"  
✅ План имеет `free_requests_limit=10` бесплатных запросов  
✅ Создаётся `BillingAccount` со статусом `TRIALING`  
✅ Создаётся организация и связывается с пользователем  

**Текущее состояние в БД**:
```sql
-- Планы в системе:
8|FREE 5 requests|SUBSCRIPTION|MONTHLY|0|0|5|
9|one time 20e 1 day|ONE_TIME|MONTHLY|20|1000|0|10
10|10e Month Subscription + 1 day free|SUBSCRIPTION|MONTHLY|10|1000|0|

-- Пользователь с активной подпиской:
1|Maks Guzz's Organization|10|ACTIVE|0|0|4
```
добавить в планы флаг default
логика: новый пользователь получает этот план при регистрации
это настраивается админом, может быть как отдельным планом, так и импортированым из падл
---

## 2. 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА #1: ONE_TIME планы можно купить при активной подписке

**Файл**: [app/billing/router.py](app/billing/router.py#L240-L244)

**Проблемный код**:
```python
# For SUBSCRIPTION plans: prevent creating new subscription if active one exists
if plan.plan_type == PlanType.SUBSCRIPTION:
    if ba.paddle_subscription_id and ba.subscription_status in (SubscriptionStatus.ACTIVE, SubscriptionStatus.TRIALING):
        raise HTTPException(
            status_code=400, 
            detail="Active subscription already exists. Cancel current subscription before subscribing to a new plan."
        )
```

**🚨 ПРОБЛЕМА**: 
- Блокируется только покупка новой SUBSCRIPTION подписки
- **НЕ блокируется** покупка ONE_TIME планов при активной подписке
- Пользователь может купить "one time 20e 1 day" имея активную подписку "10e Month Subscription"

**БИЗНЕС-РИСК**: Пользователи могут обходить подписки, покупая разовые пакеты

**✅ УТОЧНЕННАЯ ЛОГИКА**:
- При **ОТСУТСТВИИ** подписки: можно докупать ONE_TIME кредиты
- Кредиты **суммируются**: купил 10, использовал 5, остаток 5, купил еще 10 → остаток 15
- При **активной подписке**: НЕЛЬЗЯ покупать ONE_TIME кредиты
- Простая логика: либо подписка, либо разовые кредиты, но не вместе

---

## 3. 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА #2: ONE_TIME план уничтожает активную подписку

**Файл**: [app/webhooks/router.py](app/webhooks/router.py#L562-L566)

**Проблемный код**:
```python
if plan and plan.plan_type == PlanType.ONE_TIME:
    # ...
    # Set current plan to this one-time plan
    billing_account.subscription_plan_id = plan.id  # ❌ ПЕРЕЗАПИСЫВАЕТ ПОДПИСКУ!
    billing_account.subscription_status = SubscriptionStatus.ACTIVE
    billing_account.subscription_start_date = datetime.utcnow()
```

**🚨 ПРОБЛЕМА**: 
- При обработке webhook'а ONE_TIME покупки система **ПЕРЕЗАПИСЫВАЕТ** `subscription_plan_id`
- Активная месячная подписка пользователя **ТЕРЯЕТСЯ**
- `paddle_subscription_id` остается, но план уже ссылается на ONE_TIME

**БИЗНЕС-РИСК**: Потеря recurring доходов от подписок

**✅ ПРАВИЛЬНАЯ ЛОГИКА**: 
- При покупке подписки: ONE_TIME кредиты **сохраняются** но **заморожены**
- При отмене подписки: ONE_TIME кредиты снова становятся активными
- Webhook НЕ должен перезаписывать `subscription_plan_id` для ONE_TIME покупок
---

## 4. 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА #3: Неправильная логика счётчиков использования

**Файл**: [app/policy/engine.py](app/policy/engine.py#L175-L178)

**Проблемный код**:
```python
if plan.plan_type == PlanType.ONE_TIME:
    total_purchased = billing_account.one_time_purchases_count
    used = billing_account.requests_used_current_period  # ❌ НЕВЕРНЫЙ СЧЁТЧИК
    remaining = max(0, total_purchased - used)
```

**🚨 ПРОБЛЕМА**: 
- ONE_TIME и SUBSCRIPTION планы используют **ОДИН И ТОТ ЖЕ** счётчик `requests_used_current_period`
- При смене с подписки на ONE_TIME (или наоборот) счётчик сохраняется
- Пользователь может исчерпать лимит на подписке, купить ONE_TIME, но лимиты будут уже израсходованы

**Пример сценария**:
1. Пользователь на месячном плане (1000 запросов) сделал 500 запросов
2. Покупает ONE_TIME пакет на 10 запросов 
3. У него остаётся только 10-500 = **-490 запросов** (система заблокирует доступ)

---

## 5. 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА #4: Free Trial планы не связаны с агентами

**Текущее состояние БД**:
```sql
-- В plan_agents только один связанный план:
plan_id: 8, agent_id: 4  (FREE 5 requests -> basic agent)

-- Но при регистрации создается план "Free Trial" с другими параметрами
```

**🚨 ПРОБЛЕМА**: 
- При регистрации создается план "Free Trial" в коде
- Этот план **НЕ связывается** ни с одним агентом
- Пользователи не могут использовать систему даже с бесплатными запросами

---

## 6. 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА #5: Отсутствует фильтрация доступных планов

**Файл**: [app/billing/router.py](app/billing/router.py#L420-L461)

**Текущий код**:
```python
@router.get("/plans")
async def get_available_plans(db: AsyncSession = Depends(get_db)):
    """Get all available subscription plans."""
    result = await db.execute(select(SubscriptionPlan).order_by(SubscriptionPlan.price))
    plans = result.scalars().all()
    # Возвращает ВСЕ планы без фильтрации
```

**🚨 ПРОБЛЕМА**: 
- Пользователю показываются ВСЕ планы без исключений
- Включая план который у него уже активен
- Включая ONE_TIME планы (которые должны быть недоступны при активной подписке)
- Включая неактивные/сломанные планы

**БИЗНЕС-РИСК**: Плохой UX, пользователи могут купить то же самое дважды

---

## 7. 🟡 СТРУКТУРНАЯ ПРОБЛЕМА #6: Дублирование логики Paddle транзакций

**Файл**: [app/billing/router.py](app/billing/router.py#L260-L270)

**Проблемный код**:
```python
# Create Paddle transaction
# For SUBSCRIPTION plans: create subscription (recurring)
# For ONE_TIME plans: create one-time transaction
transaction = _as_dict(
    await paddle.create_subscription(  # ❌ ДЛЯ ONE_TIME ТОЖЕ SUBSCRIPTION
        customer_id=ba.paddle_customer_id,
        price_id=plan.paddle_price_id,
    )
)
```

**🟡 ПРОБЛЕМА**: 
- Для ONE_TIME планов используется `create_subscription()` вместо `create_transaction()`
- Это может приводить к неправильным webhook'ам и обработке в Paddle

---

## 8. 🟡 СТРУКТУРНАЯ ПРОБЛЕМА #7: Отсутствует история ONE_TIME покупок

**🟡 ПРОБЛЕМА**: 
- Система хранит только суммарное количество `one_time_purchases_count`
- Нет истории: когда, какой план, за сколько купили
- Невозможно предоставить пользователю детализацию покупок
- Проблемы с налогообложением и отчетностью

---

## 9. 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА #8: Нет проверки логики апгрейда

**🚨 ПРОБЛЕМА**: 
- Пользователь может "апгрейднуть" с месячного плана за $10 на недельный за $4.99
- Нет проверки на то, что новый план действительно лучше текущего
- Нет понятия "апгрейд" vs "даунгрейд"

---

## 10. 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА #9: Отсутствуют базовые валидации планов

**🚨 ПРОБЛЕМА**: При подписке НЕ проверяется:
- Активен ли план (`is_active` поле отсутствует в модели)
- Есть ли у плана хотя бы один агент
- Корректен ли `paddle_price_id`
- Не устарел ли план

это бы хорошо проверять на админской стороне
невалидные планы не должны быть показаны ползователю, я так думаю
---

## 🛠️ ПЛАН ИСПРАВЛЕНИЯ (ПРИОРИТИЗИРОВАННЫЙ)

### 🚨 СРОЧНО - КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ

**1. Блокировать ONE_TIME покупки при активной подписке**
```python
# В app/billing/router.py subscribe функции добавить:
if plan.plan_type == PlanType.ONE_TIME:
    if ba.subscription_plan_id and ba.subscription_status in (SubscriptionStatus.ACTIVE, SubscriptionStatus.TRIALING):
        current_plan = await db.execute(select(SubscriptionPlan).where(SubscriptionPlan.id == ba.subscription_plan_id))
        current_plan = current_plan.scalar_one_or_none()
        if current_plan and current_plan.plan_type == PlanType.SUBSCRIPTION:
            raise HTTPException(
                status_code=400, 
                detail="Cannot purchase credits while subscription is active. Cancel subscription first to buy additional credits."
            )
```

**2. НЕ перезаписывать subscription_plan_id для ONE_TIME покупок**
```python
# В app/webhooks/router.py НЕ устанавливать plan_id для ONE_TIME:
if plan and plan.plan_type == PlanType.ONE_TIME:
    if plan.one_time_limit:
        billing_account.one_time_purchases_count += plan.one_time_limit
        logger.info(f"Incremented one_time_purchases_count to {billing_account.one_time_purchases_count}")
    
    # ❌ УБРАТЬ ЭТИ СТРОКИ:
    # billing_account.subscription_plan_id = plan.id  
    # billing_account.subscription_status = SubscriptionStatus.ACTIVE
    
    # ✅ ТОЛЬКО ОБНОВИТЬ:
    billing_account.last_transaction_id = transaction_id
    billing_account.last_webhook_event_id = event_id
```

**3. Разделить счётчики использования**
```python
# В app/models/billing.py добавить поле:
one_time_requests_used: Mapped[int] = mapped_column(Integer, default=0, nullable=False)

# В app/policy/engine.py использовать правильный счётчик:
if plan.plan_type == PlanType.ONE_TIME:
    total_purchased = billing_account.one_time_purchases_count
    used = billing_account.one_time_requests_used  # ✅ ПРАВИЛЬНЫЙ СЧЁТЧИК
    remaining = max(0, total_purchased - used)
```

### 📅 В БЛИЖАЙШЕЕ ВРЕМЯ

**4. Исправить Free Trial планы**
- Связать Free Trial план с базовыми агентами при создании

**5. Добавить фильтрацию планов**
- Создать endpoint `/billing/plans/available` с логикой фильтрации

**6. Добавить базовые валидации**
- Проверка активности плана
- Проверка наличия агентов в плане
- Проверка корректности Paddle ID

---

## 🎯 РЕКОМЕНДАЦИИ ПО ТЕСТИРОВАНИЮ

**После исправления протестировать сценарии:**

1. **Регистрация** → проверить что Free Trial работает
2. **Подписка на месячный план** → проверить что доступ работает  
3. **Попытка купить ONE_TIME при активной подписке** → должно блокироваться
4. **Отмена подписки** → **затем** покупка ONE_TIME → должна работать
5. **Исчерпание лимитов** на каждом типе плана
6. **Webhook обработка** для обоих типов планов

---

## ⚖️ БИЗНЕС-ВЛИЯНИЕ

**Потенциальные потери**:
- 💰 Потеря recurring доходов из-за перезаписи подписок
- 😤 Негативный пользовательский опыт из-за блокировки доступа
- 🐛 Технические инциденты из-за неправильных лимитов
- 📊 Неточная аналитика из-за смешанных счётчиков

**Время на исправление**: ~4-6 часов разработки + тестирование

---

**Статус**: ❌ ТРЕБУЕТ НЕМЕДЛЕННОГО ИСПРАВЛЕНИЯ