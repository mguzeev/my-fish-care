# 📋 Таб "Usage Logs" в админ панели

## Что добавлено?

Новый таб "Usage Logs" в админ панели для просмотра всех логов API запросов.

---

## Возможности

✅ **Просмотр логов:**
- Все API запросы с метаданными
- Сортировка по умолчанию: новые логи первыми

✅ **Фильтрация:**
- По периоду времени (1 день, 7 дней, 30 дней, 90 дней)
- По названию эндпоинта или ID пользователя (поиск)
- По количеству выводимых записей (10-500)

✅ **Информация в таблице:**
- **Time** - Время запроса (локальное время)
- **Endpoint** - Адрес API эндпоинта
- **Method** - HTTP метод (GET, POST, PUT, DELETE)
- **Status** - HTTP статус код (с цветовым кодированием)
- **Response Time** - Время ответа в миллисекундах
- **Tokens** - Количество токенов использовано
- **Cost** - Стоимость запроса
- **User** - ID пользователя, сделавшего запрос
- **Error** - Описание ошибки (если есть)

---

## Статус коды (цветовое кодирование)

| Статус | Цвет | Значение |
|--------|------|----------|
| 2xx (200-299) | 🟢 Зелёный | Успешный запрос |
| 3xx (300-399) | 🟡 Оранжевый | Редирект |
| 4xx (400-499) | 🔴 Красный | Ошибка клиента |
| 5xx (500-599) | 🔴 Красный | Ошибка сервера |

---

## Инструкция по использованию

### 1. Открыть таб логов
```
Админ панель → Usage Logs (последний таб)
```

### 2. Установить период времени
```
Select: Last 7 days (по умолчанию)
Опции:
  - Last 24 hours
  - Last 7 days  ← Default
  - Last 30 days
  - Last 90 days
```

### 3. Установить количество записей
```
Input: 100 (по умолчанию)
Диапазон: 10-500
```

### 4. Поиск
```
Input: Search by endpoint or user...
Примеры:
  - /agents/invoke
  - 123 (user_id)
  - error (в описании ошибки)
```

### 5. Обновить логи
```
Button: Refresh
Или автоматически при изменении параметров
```

---

## API Endpoint

**Используемый endpoint:**
```
GET /billing/usage-records?days=7&limit=100
```

**Параметры запроса:**
- `days` - Количество дней для фильтрации (1-90)
- `limit` - Максимум записей для вывода (10-500)

**Ответ (пример):**
```json
[
  {
    "id": 1,
    "user_id": 123,
    "endpoint": "/agents/1/invoke",
    "method": "POST",
    "status_code": 200,
    "response_time_ms": 1234,
    "total_tokens": 150,
    "cost": "0.0025",
    "error_message": null,
    "created_at": "2024-01-13T14:32:10"
  },
  ...
]
```

---

## Примеры использования

### Пример 1: Поиск медленных запросов
```
1. Отфильтровать по периоду: Last 24 hours
2. Отсортировать (уже отсортированы по новым первым)
3. Искать медленные (Response Time > 1000ms)
4. Искать ошибки (Status > 400)
```

### Пример 2: Проверить использование пользователя
```
1. Введить ID пользователя в поиск
2. Увидеть все запросы этого пользователя
3. Посчитать общее использование токенов
```

### Пример 3: Найти ошибки
```
1. Отфильтровать по периоду
2. Искать в таблице Status >= 400
3. Кликнуть на Error для подробностей
```

---

## Технические детали

### Файлы, которые были изменены

#### 1. `app/templates/admin.html`
**Добавлено:**
- Кнопка таба: "Usage Logs" (строка 41)
- HTML секция таба с фильтрами (после llm-models)
- Обработчик в switchTab(): case 'logs'
- 3 функции JavaScript:
  - `loadLogs()` - загрузка логов с API
  - `displayLogs()` - отображение таблицы
  - `filterLogs()` - поиск по текст

#### 2. `app/static/css/admin.css`
**Добавлено:**
- `.logs-filters` - стили для фильтров
- `.status-badge` - цветовые коды статусов
- `.data-grid` - стили таблицы
- `.no-data`, `.error` - стили сообщений

---

## Функциональность JavaScript

### `loadLogs()`
```javascript
async function loadLogs() {
    // 1. Получить параметры из формы
    days = document.getElementById('logsDays').value
    limit = document.getElementById('logsLimit').value
    
    // 2. Запрос к API: /billing/usage-records
    response = await apiCall(`/billing/usage-records?days=${days}&limit=${limit}`)
    
    // 3. Сортировка по created_at DESC (новые первыми)
    allLogs = response.sort((a, b) => dateB - dateA)
    
    // 4. Отображение в таблице
    displayLogs(allLogs)
}
```

### `displayLogs(logs)`
```javascript
function displayLogs(logs) {
    // 1. Создать HTML таблицу
    // 2. Для каждого лога вывести строку с данными
    // 3. Цветовое кодирование статусов
    // 4. Форматирование времени
    // 5. Форматирование стоимости (4 знака после запятой)
}
```

### `filterLogs()`
```javascript
function filterLogs() {
    // 1. Получить текст поиска
    searchText = document.getElementById('logsSearch').value
    
    // 2. Отфильтровать allLogs по:
    //    - endpoint (contains searchText)
    //    - user_id (contains searchText)
    //    - error_message (contains searchText)
    
    // 3. Отображение отфильтрованных результатов
}
```

---

## Требования

### Backend API
✅ Требуется endpoint: `GET /billing/usage-records`  
✅ Этот endpoint уже существует в коде!  
✅ Находится в: `app/billing/router.py`

### Таблица БД
✅ Требуется таблица: `usage_records`  
✅ Эта таблица уже существует!  
✅ Заполняется миддлварем: `app/usage/tracker.py`

---

## Сортировка по умолчанию

**Важно:** Логи сортируются по `created_at DESC` (новые первыми)

```javascript
// В функции loadLogs():
allLogs = response.sort((a, b) => {
    const dateA = new Date(a.created_at);
    const dateB = new Date(b.created_at);
    return dateB - dateA;  // ← DESC (новые первыми)
});
```

Результат:
```
Время логирования:
  1. 2024-01-13 14:32:10  ← ПЕРВЫЙ в таблице (новый)
  2. 2024-01-13 14:32:05
  3. 2024-01-13 14:32:00
  4. 2024-01-13 14:31:55  ← ПОСЛЕДНИЙ в таблице (старый)
```

---

## Стили и дизайн

### Цветовая схема
```css
--admin-primary: #2563eb       /* Голубой */
--admin-danger: #dc2626        /* Красный */
--admin-success: #16a34a       /* Зелёный */
--admin-warning: #ea580c       /* Оранжевый */
--admin-text: #1f2937          /* Тёмный текст */
--admin-text-light: #6b7280    /* Светлый текст */
```

### Адаптивность
```css
/* На мобильных устройствах */
@media (max-width: 768px) {
    .logs-filters {
        flex-direction: column;
    }
    
    .data-grid {
        font-size: 0.75rem;
    }
}
```

---

## Примеры статусов в таблице

| Status | Цвет | Пример |
|--------|------|--------|
| 200 | 🟢 | GET /auth/me |
| 201 | 🟢 | POST /agents |
| 301 | 🟡 | Редирект |
| 400 | 🔴 | POST /agents (bad request) |
| 401 | 🔴 | GET /admin (unauthorized) |
| 404 | 🔴 | GET /nonexistent |
| 500 | 🔴 | POST /agents (server error) |

---

## Проверка работы

### 1. Убедиться, что таб видео
```
Админ панель → Последний таб должен быть "Usage Logs"
```

### 2. Открыть таб и дождаться загрузки
```
Должна появиться таблица с данными
```

### 3. Проверить сортировку
```
Первый лог должен быть с самым свежим временем
```

### 4. Попробовать поиск
```
Введить название эндпоинта (например /auth/me)
Таблица должна отфильтроваться
```

### 5. Проверить фильтры
```
Изменить период времени → должны загрузиться новые логи
Изменить лимит → должно измениться количество строк
```

---

## Возможные проблемы и решения

### Проблема: "Таб не видно"
```
Решение: Проверить, что файл admin.html сохранен
Обновить страницу браузера (F5 или Ctrl+Shift+R)
```

### Проблема: "Логи не загружаются"
```
Решение: Проверить консоль браузера (F12 → Console)
Проверить, что API endpoint /billing/usage-records работает
Проверить, что authentication токен валиден
```

### Проблема: "Таблица выглядит странно"
```
Решение: Очистить кэш браузера (Ctrl+Shift+Del)
Обновить страницу
Проверить, что CSS файл загружен (F12 → Network)
```

### Проблема: "Поиск не работает"
```
Решение: Проверить, что вводить текст в input "Search..."
Функция filterLogs() чувствительна к регистру (case-insensitive)
```

---

## Будущие улучшения

### Возможные добавления:
- [ ] Экспорт логов в CSV
- [ ] Графики использования (canvas/chart.js)
- [ ] Фильтр по статус кодам
- [ ] Сортировка по столбцам (клик на заголовок)
- [ ] Пагинация для больших наборов данных
- [ ] Фильтр по методу (GET, POST и т.д.)
- [ ] Фильтр по диапазону цены
- [ ] Real-time обновление логов

---

## Заключение

✅ Таб "Usage Logs" успешно добавлен в админ панель  
✅ Сортировка по умолчанию: новые логи первыми  
✅ Полная фильтрация и поиск  
✅ Цветовое кодирование статусов  
✅ Готово к использованию  

**Можно использовать прямо сейчас!** 🎉
